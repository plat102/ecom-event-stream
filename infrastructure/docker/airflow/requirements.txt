# Pinned to the base image so no provider drags a different Airflow in.
apache-airflow==2.10.4

apache-airflow-providers-postgres
apache-airflow-providers-common-sql
apache-airflow-providers-apache-kafka
apache-airflow-providers-discord
apache-airflow-providers-http

# The orchestration tests import airflow, so they run in this image.
pytest

# Used by the shared connectors the `dec` hooks wrap.
confluent-kafka
psycopg2-binary
